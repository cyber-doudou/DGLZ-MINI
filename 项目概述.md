# 项目概述：Da Guai Lu Zi (大怪路子) Web 游戏

## 1. 项目简介
本项目是一个基于 Web 的 6 人多人在线扑克游戏，实现了流行于中国上海地区的经典扑克玩法——“大怪路子”（Da Guai Lu Zi）。游戏支持 3v3 组队对战模式，并且深度还原了该玩法中的”打级”和”进贡/还贡”等特色规则。项目内置全自动自我进化 AI，无需人工干预即可持续提升策略水平。

## 2. 技术栈架构 (Tech Stack)

### 前端 (Frontend)
- **核心框架**: React 19 + TypeScript + Vite 构建
- **样式系统**: TailwindCSS v4，提供现代化且响应式的 UI 设计。
- **动画与交互**: 
  - `motion` (Framer Motion) 用于平滑的 UI 动画。
  - `@hello-pangea/dnd` 用于卡牌等元素的拖放交互操作。
  - `lucide-react` 提供精致的矢量图标。
- **状态与通信**: `socket.io-client` 配合自定义 Hook (`useGameSocket`) 实现与后端的实时双向通信。

### 后端 (Backend)
- **核心框架**: Node.js 环境下的 Express.js + `tsx` (直接运行 TypeScript 服务端代码 `server.ts`)。
- **实时通信**: `socket.io` (WebSocket) 负责广播游戏状态变更。
- **状态存储**: 采用基于内存的状态管理 (In-memory Game State) 追踪每个房间的对象状态与游戏对局记录。
- **AI 决策**: 本地评分引擎（`StrategicPlanner`）负责零延迟出牌决策；`@google/generative-ai` 作为可选的赛后深度分析补充。

## 3. 核心功能与模块 (Core Features)

### 多人房间系统 (Room Management)
- 支持玩家创建大厅并生成 6 位唯一字符串的邀请码 (`roomId`)。
- 允许最多 6 位玩家加入，区分对战阵营（队伍 0 和 队伍 1）。
- 具备房主权限控制（启动游戏），并支持房内发送实时 Emoji 表情互动。

### 实时游戏同步 (Real-time Game State)
- 使用 `GameState` 接口统一定义游戏进度，分为 `Waiting`（等待）、`Playing`（对局中）、`Tribute`（进贡中） 和 `Finished`（已结算） 四个核心阶段。
- 采用服务端权威 (Server-Authoritative) 模式，客户端的任何出牌操作通过事件 (`playCards`, `returnTribute` 等) 发送到服务端进行规则校验。

### 规则引擎 (Rule Engine)
核心规则抽离到 `src/rules/DaGuaiLuZiRule.ts` 中：
- 管理包含 3 副牌（162 张牌）在内的大怪路子牌堆。
- 洗牌与发牌逻辑。
- 卡牌组合验证（如单张、对子、三张、炸弹的判定与比大小）。
- 升级机制（例如从 2 打到 A，根据本局被”关”的玩家数量决定跳级数）。

### “进贡”机制 (Tribute System)
- 当某一队伍在上一局实现”关人”（即敌方有玩家未出完牌）时，本局开局将触发独立的 Tribute 流程。
- 系统自动判定输家向赢家的最大牌进贡（Payer -> Receiver），并在之后通过界面进行还贡（Receiver 挑选卡牌返还），完全模拟真实的线下博弈体验。

### AI 决策引擎 (AI Decision Engine)
AI 采用 5 层架构，全部在本地 Node.js 运行，零延迟决策：

| 层级 | 模块 | 职责 |
|------|------|------|
| 感知层 | `HandAnalyzer` + `GameMemory` | 手牌结构拆解、对手历史追踪 |
| 概率层 | `CardProbabilityEngine` | 算牌：P(对手有炸弹)、P(出牌被管上)、怪牌剩余数 |
| 态势层 | `PhaseDetector` | 5 种游戏阶段动态权重（Opening/MidGame/Endgame/Emergency/Coasting）|
| 规划层 | `StrategicPlanner` | 前瞻评分：模拟出牌后手牌重拆，结合概率信息决策 |
| 执行层 | `BotManager` | 调度决策、进贡流程自动化、决策日志 |

### 自我进化系统 (Self-Evolution)
AI 在无人干预的情况下持续优化出牌策略：

```
服务器空闲 → SelfPlaySimulator 自动运行一批无头对战（12局/批，每5分钟）
               ↓
           WeightEvolver 统计各 phase 决策胜率
               ↓ 领域特定梯度（带动量 SGD）
           logs/ai_tuning_overrides.json（PhaseWeights 增量）
               ↓ 每次出牌时读取
           PhaseDetector.applyOverrides()
```

- **进化信号**：决策级别胜率（赢家决策数 / 总决策数），均衡自对弈中趋近 0.5，偏离即触发调参
- **替代 LLM 调参**：统计梯度替代原有的 Gemini 每局调参，速度快、结果可重现
- **双路输入**：真实对局 + 无头自对弈同时喂入进化器

## 4. 运行方式
项目已经在 `package.json` 提供了开箱即用的 NPM 脚本：
- **安装依赖**: `npm install`
- **本地开发**: `npm run dev` (通过 Vite 与 tsx 同步启动前后端)
- **访问应用**: 通常为主流的本地前端端口以及服务端的 5173 端口。

启动后服务器将在 30 秒后自动开始第一批自我对战，之后每 5 分钟（空闲时）运行一次，进化结果写入 `logs/ai_tuning_overrides.json`。
